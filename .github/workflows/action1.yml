# name: Security Pipeline

# on:
#   push:
#     branches: [feature-1]
#   pull_request:
#     branches: [feature-1]

# jobs:
#   build-and-scan:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up JDK 17
#         uses: actions/setup-java@v4
#         with:
#           java-version: '17'
#           distribution: 'temurin'

#       - name: Build with Maven
#         run: mvn clean install

#       - name: list files and foulers
#         run: ls -la && ls target
        
#       - name: Build Docker image
#         run: docker build -t java-app:latest .

#       - name: Run Trivy scan
#         uses: aquasecurity/trivy-action@master
#         with:
#           image-ref: java-app:latest
#           format: 'table'
#           output: 'trivy-report.txt'

#       - name: Upload Trivy Report
#         uses: actions/upload-artifact@v4
#         with:
#           name: trivy-report
#           path: trivy-report.txt

# Code Generated by Sidekick is for learning and experimentation purposes only.
name: Security Pipeline

on:
  push:
    branches: [feature-1]
  pull_request:
    branches: [feature-1]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn clean package

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-jar
          path: target/*.jar

        
  static-code-analysis:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: SonarQube Scan
        uses: SonarSource/sonarcloud-github-action@master
        with:
          args: >
            -Dsonar.projectKey=${{ github.repository }}
            -Dsonar.host.url=${{ secrets.SONAR_HOST_URL }}
            -Dsonar.login=${{ secrets.SONAR_TOKEN }}

      # - name: Build and analyze with SonarQube
      #   env:
      #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      #     SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      #   run: |
      #     mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar \
      #       -Dsonar.projectKey=java-app \
      #       -Dsonar.projectName='java-app' \
      #       -Dsonar.host.url=${SONAR_HOST_URL} \
      #       -Dsonar.login=${SONAR_TOKEN}


  dependency-check:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Download OWASP Dependency-Check
        run: |
          wget https://github.com/jeremylong/DependencyCheck/releases/download/v8.4.0/dependency-check-8.4.0-release.zip
          unzip dependency-check-8.4.0-release.zip

      - name: Run Dependency-Check
        run: |
          ./dependency-check/bin/dependency-check.sh --project "demo" --scan . --format "HTML" --out dependency-check-report.html

      - name: Upload Dependency-Check Report
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-report
          path: dependency-check-report.html

  container-security:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn clean install

      # - name: list files and foulders
      #   run: ls -la && ls target

      - name: Build Docker image
        run: docker build -t java-app:latest .

      - name: Run Trivy scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'java-app:latest'
          format: 'table'
          output: 'trivy-report.txt'

      - name: Upload Trivy Report
        uses: actions/upload-artifact@v4
        with:
          name: trivy-report
          path: trivy-report.txt

  # upload-reports-s3:
  #   runs-on: ubuntu-latest
  #   needs: [dependency-check, container-security]
  #   steps:
  #     - name: Download Dependency-Check Report
  #       uses: actions/download-artifact@v3
  #       with:
  #         name: dependency-check-report
  #         path: reports

  #     - name: Download Trivy Report
  #       uses: actions/download-artifact@v3
  #       with:
  #         name: trivy-report
  #         path: reports

  #     - name: Configure AWS Credentials
  #       uses: aws-actions/configure-aws-credentials@v2
  #       with:
  #         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         aws-region: ${{ secrets.AWS_REGION }}

  #     - name: Upload reports to S3
  #       run: |
  #         aws s3 cp reports/dependency-check-report.html s3://${{ secrets.S3_BUCKET }}/dependency-check-report.html
  #         aws s3 cp reports/trivy-report.txt s3://${{ secrets.S3_BUCKET }}/trivy-report.txt

